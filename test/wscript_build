#! /usr/bin/env python
# encoding: utf-8

bld.program(
    features = 'cxx test',
    source   = ['cpuid_tests.cpp', 'commandline_arguments.cpp']
                + bld.path.ant_glob('src/test_*.cpp'),
    target   = 'cpuid_tests',
    use = ['cpuid','gtest','boost_includes','boost_program_options'])
